name: 'Setup'
description: 'Install the mise-managed toolchain and project dependencies'

runs:
  using: 'composite'
  steps:
    - name: Install mise
      uses: jdx/mise-action@v2

    - name: Install deps
      shell: bash
      run: pnpm install --frozen-lockfile
